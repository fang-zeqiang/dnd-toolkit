# CONTEXT

DND Toolkit 领域术语表。只记语言，不记实现。

## 术语

### 工具 (Tool)
一个独立、零依赖、可直接在浏览器运行的桌边应用（如酒馆经营、战图印制坊）。每个工具是一个自包含的 HTML 页面，互相之间不共享运行时代码。

### Wiki（不全书）
《5E 不全书》中文规则合集在本站的在线阅读区。内容源是 CHM 原书，站内呈现为「阅读器 + 原书页面」。与「工具」不同：Wiki 是只读内容，不是交互应用。

### 阅读器 (Reader)
Wiki 的外壳页面：左侧目录树 + 右侧内容区。目录树来自原书目录（.hhc），内容区展示原书页面。原书页面本身不属于阅读器。

### 原书页面 (Book Page)
从 CHM 解压出的单篇 HTML。原则：除编码转换外不重新格式化、不改写内容。

### PIN 门禁 (PIN Gate)
Wiki 区的访问口令。目的是「不对公网裸露」而非强安全——防搜索引擎与路人，不防有心人。

### 主站 (Landing)
`dnd.zeqiang.fun` 首页，工具与 Wiki 的入口。工具公开，Wiki 过 PIN 门禁。
